{
  "name": "quote-api",
  "version": "1.0.0",
  "description": "A small quote-serving JSON API.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --check server.js && node --check utils.js && node --check scripts.js && node --check public/js/script.js && node --check public/js/add-quote.js && node --check public/add-quote.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.22.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint-plugin-react-hooks": "^7.1.1"
  },
  "repository": {
    "type": "git",
    "url": "quote-api"
  },
  "keywords": [
    "node",
    "api"
  ]
}
